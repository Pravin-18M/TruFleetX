# ─── Supabase ────────────────────────────────────────────────────────────────
# Project URL — found at: Supabase Dashboard → Project Settings → API → Project URL
SUPABASE_URL=https://lqwtkwjoydyngeqnrttv.supabase.co

# Service Role Key (SECRET) — NOT the anon key.
# Found at: Supabase Dashboard → Project Settings → API → service_role → Reveal
# This key bypasses Row Level Security, which is correct for a trusted backend.
SUPABASE_SERVICE_KEY=sb_publishable_GvscF5wXLsIR-vl79vcT9Q_C-0QjCfd

# ─── JWT ─────────────────────────────────────────────────────────────────────
# Any long random string. Example: openssl rand -base64 32
JWT_SECRET=your_super_secret_jwt_key_here_change_this

# ─── Server ──────────────────────────────────────────────────────────────────
PORT=3000
